
Private Sub Polecenie1_Click()
Dim db As DAO.Database
Dim zas As DAO.Recordset
Dim odp As Integer

Set db = CurrentDb
Set zas = db.OpenRecordset("Skladniki", db.OpenTable)
odp = MsgBox("Liczba wierszy w tablli skladniki = " & zas.RecordCount, vbOROnly, "Liczba wierszy Skladniki")

End Sub
